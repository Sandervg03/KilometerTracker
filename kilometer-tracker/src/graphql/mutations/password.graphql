mutation InsertPassword(
  $email: String!
  $password: String!
) {
  insert_password(
    objects: [
      {
        email: $email
        password: $password
      }
    ]
  ) {
    affected_rows
  }
}