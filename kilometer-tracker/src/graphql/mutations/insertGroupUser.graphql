mutation InsertGroupUser($groupId: uuid, $userEmail: String) {
  insert_group_users(
    objects: [
      {
        group: $groupId
        email: $userEmail
      }
    ]
  ) {
    affected_rows
  }
}
