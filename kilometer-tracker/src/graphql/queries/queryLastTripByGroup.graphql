query LastTripByGroup($group: uuid!) {
  trips(
    where: { group: { _eq: $group } }
    order_by: { current_mileage: desc }
    limit: 1
  ) {
    current_mileage
    email
    filled_tank
    group
    cost
  }
}