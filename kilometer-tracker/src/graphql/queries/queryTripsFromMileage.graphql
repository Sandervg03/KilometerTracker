query TripsFromMileage($group: uuid!, $mileage: Int!) {
  trips(
    where: { group: { _eq: $group }, current_mileage: { _gt: $mileage } }
    order_by: { current_mileage: asc }
  ) {
    current_mileage
    email
    filled_tank
    group
    cost
  }
}
